import os
import sys
THIS_DIR = os.path.dirname(os.path.realpath(__file__))
sys.path.append(THIS_DIR + "/..")
from config import master_config

class Coord(process):
    def setup():
        pass

    def run():
        await(len(listof(p, received(('RequestFromClient', "Done"), from_=p))) == 1)

    def receive(msg=('RequestFromClient', R), from_=p):
        print("Request from Client: " + R)
        send(('ResponseFromCoord', "shah"), to=p)
